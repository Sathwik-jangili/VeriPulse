# Optional. For `npm run dev` / `vite preview` with proxy: leave commented (app uses /api).
#
# Use only if the UI and API are on different origins and you are NOT using Vite's /api proxy:
# VITE_API_BASE=http://127.0.0.1:5000
#
# Wrong (will 404): VITE_API_BASE=http://127.0.0.1:5000/api
