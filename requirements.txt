pandas>=1.0.0
scikit-learn>=1.0.0
numpy>=1.20.0
langdetect>=1.0.9
# Optional: Gemini batch labeling (scripts/label_live_gemini_fakeddit_style.py)
google-generativeai>=0.8.0

# Backend + Advanced Hybrid inference (VeriPulse):
# pip install torch transformers flask flask-cors
#
# For transformer_fakeddit.py / training (optional):
# pip install datasets accelerate
